# 福彩3D 智能分析系统 - 使用指南

## 项目概述

本项目是一个基于 Streamlit 的福彩3D智能分析网页应用，提供以下功能：

- **实时数据抓取**：直接从官网抓取最新开奖数据，不依赖本地数据库
- **智能缓存**：使用 `st.cache_data` 和 `st.cache_resource` 优化性能
- **遗漏分析**：计算每个数字的遗漏期数
- **冷热分析**：统计近期数字出现频率，识别热号和冷号
- **组选三分析**：计算组选三的遗漏情况
- **概率计算**：基于组合数学计算中奖概率
- **可视化图表**：使用 Plotly 生成交互式图表
- **云端兼容**：支持 Streamlit Cloud 部署，无需本地 `.db` 文件

---

## 1. 安装 Python 依赖库

### 方式一：使用 requirements.txt（推荐）

```bash
pip install -r requirements.txt
```

### 方式二：逐个安装

```bash
pip install streamlit>=1.28.0
pip install plotly>=5.17.0
pip install pandas>=2.0.0
pip install requests>=2.31.0
pip install beautifulsoup4>=4.12.0
pip install schedule>=1.2.0
pip install urllib3>=2.0.0
```

### 依赖库说明

| 库名称 | 版本要求 | 用途 |
|--------|----------|------|
| **streamlit** | >=1.28.0 | 构建交互式网页应用 |
| **plotly** | >=5.17.0 | 生成交互式可视化图表 |
| **pandas** | >=2.0.0 | 数据处理和分析 |
| **requests** | >=2.31.0 | HTTP 请求，抓取网页数据 |
| **beautifulsoup4** | >=4.12.0 | HTML 解析，提取开奖数据 |
| **schedule** | >=1.2.0 | 定时任务（本地使用） |
| **urllib3** | >=2.0.0 | HTTP 库（requests 依赖） |

> **注意**：`sqlite3`、`math`、`re`、`time` 是 Python 标准库，无需额外安装。

### 验证安装是否成功

```bash
pip list | findstr streamlit
pip list | findstr requests
pip list | findstr beautifulsoup4
```

---

## 2. 在本地启动 Streamlit 网页

### 步骤 1：进入项目目录

```bash
cd F:\lq-lh\3d
```

### 步骤 2：启动应用

```bash
streamlit run app.py
```

### 步骤 3：访问网页

启动成功后，终端会显示类似以下信息：

```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

在浏览器中打开 **http://localhost:8501** 即可访问应用。

### 快速启动脚本（可选）

在项目目录创建 `start.bat` 文件，双击即可启动：

```batch
@echo off
cd /d F:\lq-lh\3d
streamlit run app.py
pause
```

---

## 3. 让网页在后台持续运行

### 方式一：使用 `streamlit run` 的后台模式（推荐）

#### Windows PowerShell

```powershell
Start-Process -NoNewWindow -FilePath "streamlit" -ArgumentList "run", "app.py"
```

#### 使用 `pythonw.exe`（完全后台，无控制台窗口）

```bash
pythonw -m streamlit run app.py
```

### 方式二：使用 `nohup` 风格（Windows 替代方案）

创建 `run_background.bat`：

```batch
@echo off
start /b streamlit run app.py
echo Streamlit 已在后台启动！
echo 访问地址: http://localhost:8501
```

### 方式三：使用 Windows 任务计划程序（开机自启）

1. 打开 **任务计划程序**
2. 创建基本任务
3. 设置触发器为"计算机启动时"
4. 操作设置为启动程序：
   - 程序：`streamlit`
   - 参数：`run app.py`
   - 起始于：`F:\lq-lh\3d`

### 方式四：使用 Python 的 `subprocess` 脚本

创建 `start_background.py`：

```python
import subprocess
import sys

subprocess.Popen(
    [sys.executable, "-m", "streamlit", "run", "app.py"],
    creationflags=subprocess.CREATE_NO_WINDOW
)
print("Streamlit 已在后台启动！")
print("访问地址: http://localhost:8501")
```

运行：

```bash
python start_background.py
```

---

## 4. 停止后台运行的 Streamlit

### 查找进程

```bash
tasklist | findstr streamlit
```

### 停止进程

```bash
taskkill /F /IM streamlit.exe
```

或者停止所有 Python 进程：

```bash
taskkill /F /IM python.exe
```

---

## 5. 常见问题

### Q1: 端口 8501 被占用怎么办？

```bash
streamlit run app.py --server.port 8502
```

### Q2: 如何让局域网内其他设备访问？

```bash
streamlit run app.py --server.address 0.0.0.0
```

### Q3: 如何设置自动重新加载？

Streamlit 默认开启自动重载，修改代码后保存即可自动刷新。

如需关闭：

```bash
streamlit run app.py --server.runOnSave false
```

### Q4: 数据库文件在哪里？

数据库文件位于项目根目录：`lottery_data.db`

> **注意**：在 Streamlit Cloud 环境中，程序会直接从官网实时抓取数据，不依赖本地 `.db` 文件。

### Q5: 云端部署报错 `ModuleNotFoundError: No module named 'requests'`

这是因为云端环境缺少依赖库。确保 `requirements.txt` 文件包含所有必要的库：

```
streamlit>=1.28.0
plotly>=5.17.0
pandas>=2.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
schedule>=1.2.0
urllib3>=2.0.0
```

---

## 6. 项目文件结构

```
F:\lq-lh\3d\
├── app.py              # Streamlit 主程序（已优化云端兼容）
├── analyzer.py         # 数据分析模块
├── scraper_3d.py       # 数据爬取模块（本地使用）
├── lottery_data.db     # SQLite 数据库（本地缓存）
├── requirements.txt    # Python 依赖列表（云端必需）
└── 使用指南.md          # 本文件
```

---

## 7. 完整操作命令速查表

| 操作 | 命令 |
|------|------|
| 安装依赖 | `pip install -r requirements.txt` |
| 启动应用 | `streamlit run app.py` |
| 指定端口启动 | `streamlit run app.py --server.port 8502` |
| 后台启动 | `pythonw -m streamlit run app.py` |
| 查看进程 | `tasklist \| findstr streamlit` |
| 停止进程 | `taskkill /F /IM streamlit.exe` |
| 验证安装 | `pip list \| findstr streamlit` |

---

## 8. 功能效果说明

### 已实现功能

1. **实时数据抓取**：直接从官网抓取近一年开奖数据，无需本地数据库
2. **智能缓存**：使用 `@st.cache_data(ttl=3600)` 缓存数据 1 小时，减少重复请求
3. **遗漏分析**：计算0-9每个数字的当前遗漏期数
4. **冷热分析**：统计最近30期数字出现频率
5. **组选三分析**：计算组选三形态的遗漏情况
6. **概率计算**：基于组合数学计算不同玩法的中奖概率
7. **可视化展示**：使用 Plotly 生成交互式图表
8. **宽屏布局**：采用 `layout="wide"` 提供更好的视觉体验
9. **云端兼容**：支持 Streamlit Cloud 部署，关机后手机也能访问最新数据

### 云端优化说明

- **数据加载逻辑**：优先尝试读取本地 `.db` 文件，如果不存在或数据不是最新，则直接从官网实时抓取
- **内存缓存**：使用 `st.session_state` 和 `@st.cache_data` 将数据保存在内存中，避免频繁请求
- **自动更新**：检测到数据不是最新时，自动触发爬虫抓取最新数据
- **进度显示**：抓取数据时显示进度条和状态提示，提升用户体验

### 访问地址

- 本地访问：`http://localhost:8501`
- 局域网访问：`http://<你的IP地址>:8501`
- 云端访问：`https://<你的应用名称>.streamlit.app`

---

## 9. 云端部署指南

### 步骤 1：准备 GitHub 仓库

1. 在 GitHub 创建新仓库
2. 将所有文件推送到仓库：
   ```bash
   git add .
   git commit -m "福彩3D智能分析系统"
   git push origin main
   ```

### 步骤 2：部署到 Streamlit Cloud

1. 访问 [share.streamlit.io](https://share.streamlit.io)
2. 使用 GitHub 账号登录
3. 点击 "New app"
4. 选择你的仓库、分支和主文件（`app.py`）
5. 点击 "Deploy"

### 步骤 3：验证部署

- 部署成功后，访问生成的 URL
- 确认数据能正常加载（从官网实时抓取）
- 测试所有功能是否正常

### 注意事项

- 确保 `requirements.txt` 包含所有依赖库
- 云端环境磁盘是临时的，不要依赖本地 `.db` 文件
- 使用 `@st.cache_data` 缓存数据，减少 API 请求频率
- 避免频繁抓取数据，遵守网站 robots.txt 规则

---

## 10. 故障排除

### 问题 1：❌ 无法从官网获取数据

**原因分析：**
- 官网服务器可能暂时不可用
- 云端环境网络限制（部分云服务器无法访问外部网站）
- 请求频率过高被临时限制

**解决方案：**

#### 方案 A：等待后重试
1. 等待 1-2 分钟
2. 点击页面右下角的 "↻ Rerun" 按钮
3. 或点击错误提示中的 "🔄 重新尝试加载数据" 按钮

#### 方案 B：使用本地数据库
如果你在本地有 `lottery_data.db` 文件，程序会自动读取：
```bash
# 在本地运行爬虫
python scraper_3d.py --full
```

#### 方案 C：测试网络连接
运行测试脚本检查网络：
```bash
python test_connection.py
```

### 问题 2：云端部署后数据不更新

**原因：** Streamlit Cloud 的缓存机制

**解决方案：**
- 缓存时间为 1 小时（3600 秒）
- 手动清除缓存：点击页面右上角 "⋮" → "Clear cache"
- 重新部署应用

### 问题 3：本地运行正常，云端报错

**常见原因：**
1. **网络限制**：云端服务器可能无法访问 `kaijiang.zhcw.com`
2. **依赖缺失**：确保 `requirements.txt` 包含所有库
3. **文件路径**：云端使用 Linux 路径，确保使用相对路径

**解决方案：**
- 检查 Streamlit Cloud 日志
- 确保 `requirements.txt` 完整
- 使用相对路径（如 `lottery_data.db` 而非 `F:\lq-lh\3d\lottery_data.db`）

---

**最后更新**: 2026-08-08