# 福彩3D 智能分析系统 - 使用指南

## 项目概述

本项目是一个基于 Streamlit 的福彩3D智能分析网页应用，提供以下功能：

- **历史数据展示**：从数据库加载福彩3D历史开奖记录
- **遗漏分析**：计算每个数字的遗漏期数
- **冷热分析**：统计近期数字出现频率，识别热号和冷号
- **组选三分析**：计算组选三的遗漏情况
- **概率计算**：基于组合数学计算中奖概率
- **可视化图表**：使用 Plotly 生成交互式图表

---

## 1. 安装 Python 依赖库

### 方式一：使用 requirements.txt（推荐）

```bash
pip install -r requirements.txt
```

### 方式二：逐个安装

```bash
pip install streamlit>=1.28.0
pip install plotly>=5.17.0
pip install pandas>=2.0.0
```

### 依赖库说明

| 库名称 | 版本要求 | 用途 |
|--------|----------|------|
| **streamlit** | >=1.28.0 | 构建交互式网页应用 |
| **plotly** | >=5.17.0 | 生成交互式可视化图表 |
| **pandas** | >=2.0.0 | 数据处理和分析 |

> **注意**：`sqlite3` 和 `math` 是 Python 标准库，无需额外安装。

### 验证安装是否成功

```bash
pip list | findstr streamlit
pip list | findstr plotly
pip list | findstr pandas
```

---

## 2. 在本地启动 Streamlit 网页

### 步骤 1：进入项目目录

```bash
cd F:\lq-lh\3d
```

### 步骤 2：启动应用

```bash
streamlit run app.py
```

### 步骤 3：访问网页

启动成功后，终端会显示类似以下信息：

```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

在浏览器中打开 **http://localhost:8501** 即可访问应用。

### 快速启动脚本（可选）

在项目目录创建 `start.bat` 文件，双击即可启动：

```batch
@echo off
cd /d F:\lq-lh\3d
streamlit run app.py
pause
```

---

## 3. 让网页在后台持续运行

### 方式一：使用 `streamlit run` 的后台模式（推荐）

#### Windows PowerShell

```powershell
Start-Process -NoNewWindow -FilePath "streamlit" -ArgumentList "run", "app.py"
```

#### 使用 `pythonw.exe`（完全后台，无控制台窗口）

```bash
pythonw -m streamlit run app.py
```

### 方式二：使用 `nohup` 风格（Windows 替代方案）

创建 `run_background.bat`：

```batch
@echo off
start /b streamlit run app.py
echo Streamlit 已在后台启动！
echo 访问地址: http://localhost:8501
```

### 方式三：使用 Windows 任务计划程序（开机自启）

1. 打开 **任务计划程序**
2. 创建基本任务
3. 设置触发器为"计算机启动时"
4. 操作设置为启动程序：
   - 程序：`streamlit`
   - 参数：`run app.py`
   - 起始于：`F:\lq-lh\3d`

### 方式四：使用 Python 的 `subprocess` 脚本

创建 `start_background.py`：

```python
import subprocess
import sys

subprocess.Popen(
    [sys.executable, "-m", "streamlit", "run", "app.py"],
    creationflags=subprocess.CREATE_NO_WINDOW
)
print("Streamlit 已在后台启动！")
print("访问地址: http://localhost:8501")
```

运行：

```bash
python start_background.py
```

---

## 4. 停止后台运行的 Streamlit

### 查找进程

```bash
tasklist | findstr streamlit
```

### 停止进程

```bash
taskkill /F /IM streamlit.exe
```

或者停止所有 Python 进程：

```bash
taskkill /F /IM python.exe
```

---

## 5. 常见问题

### Q1: 端口 8501 被占用怎么办？

```bash
streamlit run app.py --server.port 8502
```

### Q2: 如何让局域网内其他设备访问？

```bash
streamlit run app.py --server.address 0.0.0.0
```

### Q3: 如何设置自动重新加载？

Streamlit 默认开启自动重载，修改代码后保存即可自动刷新。

如需关闭：

```bash
streamlit run app.py --server.runOnSave false
```

### Q4: 数据库文件在哪里？

数据库文件位于项目根目录：`lottery_data.db`

---

## 6. 项目文件结构

```
F:\lq-lh\3d\
├── app.py              # Streamlit 主程序
├── analyzer.py         # 数据分析模块
├── scraper_3d.py       # 数据爬取模块
├── lottery_data.db     # SQLite 数据库
├── requirements.txt    # Python 依赖列表
└── 使用指南.md          # 本文件
```

---

## 7. 完整操作命令速查表

| 操作 | 命令 |
|------|------|
| 安装依赖 | `pip install -r requirements.txt` |
| 启动应用 | `streamlit run app.py` |
| 指定端口启动 | `streamlit run app.py --server.port 8502` |
| 后台启动 | `pythonw -m streamlit run app.py` |
| 查看进程 | `tasklist \| findstr streamlit` |
| 停止进程 | `taskkill /F /IM streamlit.exe` |
| 验证安装 | `pip list \| findstr streamlit` |

---

## 8. 功能效果说明

### 已实现功能

1. **数据加载**：从 SQLite 数据库读取福彩3D历史开奖数据
2. **遗漏分析**：计算0-9每个数字的当前遗漏期数
3. **冷热分析**：统计最近30期数字出现频率
4. **组选三分析**：计算组选三形态的遗漏情况
5. **概率计算**：基于组合数学计算不同玩法的中奖概率
6. **可视化展示**：使用 Plotly 生成交互式图表
7. **宽屏布局**：采用 `layout="wide"` 提供更好的视觉体验

### 访问地址

- 本地访问：`http://localhost:8501`
- 局域网访问：`http://<你的IP地址>:8501`

---

**最后更新**: 2026-08-08